<h1>タスク一覧</h1>

<ul>
  <% @tasks.each do |tasks| %>
  <li>
    <!--タスクid-->
    <%= link_to tasks.id, task_path(tasks) %>:
    <!--タスク-->
    <%= tasks.content %> &gt; <%= tasks.status %>
  </li>
  <% end %>
</ul>
<%= paginate @tasks %>

<%= link_to'新規タスク作成', new_task_path%>