<h1>id:<%= @task.id %>のタスク詳細ページ</h1>

<table class="table table-bordered">
  <tr>
    <th>タスク内容</th>
    <td><%= @task.content %></td>
  </tr>
  <tr>
    <th>進行状態</th>
    <td><%= @task.status %></td>
  </tr>
</table>

<p>
  <%= link_to 'このタスクを編集する', edit_task_path(@task), class: 'btn btn-default'%>
  <%= link_to 'このタスクを削除する', task_path(@task), method: :delete, data: {confirm: '本当に削除してよろしいですか？'}, class: 'btn btn-danger' %>
</p>
